#ifndef MENU_H
#define MENU_H

#include <iostream>

/**
 * Menu which displays user choices:
 * 1 Insert and Send data to Server
 * 2 Select all data in all DB tables
 * 3 exit
 */
class Menu
{
	
public:
	void menuChoice( );
	int selectMenuVoice();
	
private:
	
	
};

#endif
